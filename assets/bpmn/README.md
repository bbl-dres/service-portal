# BPMN-Diagramme — Prozessdokumentation Bauten

18 BPMN-2.0-Dateien der Prozesse `TQ.21.00.00.*` (Immobilienmanagement K0).

Herkunft: übernommen aus dem Prototyp `process-hub` (assets/bpmn-bbl/,
Stand 2026-08). Die Dateien wurden dort aus den PDF-Diagramm-Exporten der
BBL-Prozessdokumentation (Confluence) rekonstruiert — semantische Ebene
(Lanes, Aufgaben, Gateways, Sequenzflüsse) plus BPMN-DI-Layout. Sie sind
Demonstrationsdaten des Prototyps, keine freigegebene Prozessdokumentation.

Gelesen von `js/apps/process-docs.js` (bpmn-js-Viewer + Schrittliste);
referenziert über das Feld `bpmn` in `data/processes.json`.
